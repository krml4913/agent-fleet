# Pair review — implementer with AI peer review and user sign-off.
name: pair_review
description: Implementer with AI peer review; the user has the final say.
stages:
  - role: implementer
    agent: codex:gpt-5.5
    peer_review:
      role: code-reviewer
      agent: claude:opus
    user_approval: required
