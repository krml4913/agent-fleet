# Pair review — implementer + reviewer, user signs off.
name: pair_review
description: Implementer hands off to a separate reviewer; the user has the final say.
stages:
  - role: implementer
    agent: claude:sonnet
  - role: reviewer
    agent: claude:opus
    user_approval: required
