"""fleet subcommand modules.

Each command module exposes:
    add_parser(subparsers) -> None      # register argparse subparser
    run(args) -> int                    # execute, return process exit code
"""
